---
apiVersion: v1
kind: Service
metadata:
  name: showroom
  namespace: {{ .Values.showroom.namespace }}
spec:
  selector:
    app.kubernetes.io/name: showroom
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
